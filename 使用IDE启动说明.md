# 使用 IDE 启动后端服务（推荐方法）

如果命令行启动遇到问题（如找不到 `java` 或 `mvn` 命令），**强烈推荐使用 IDE 启动**，IDE 会自动处理所有环境配置。

## 🎯 推荐：使用 IntelliJ IDEA

### 步骤 1：打开项目

1. 打开 IntelliJ IDEA
2. 选择 `File` → `Open`
3. 选择 `d:\A_WORK\GraduationProject\done\backend` 文件夹
4. 点击 `OK`

### 步骤 2：等待 Maven 导入

- IDEA 会自动检测 `pom.xml` 并导入依赖
- 右下角会显示 "Maven projects need to be imported"，点击 `Import Maven Projects`
- 等待依赖下载完成（首次可能需要几分钟）

### 步骤 3：运行项目

1. 在项目树中找到：`src/main/java/com/parking/ParkingApplication.java`
2. **右键点击** `ParkingApplication.java`
3. 选择 `Run 'ParkingApplication.main()'`

或者：

1. 打开 `ParkingApplication.java` 文件
2. 点击类名旁边的绿色三角形 ▶️
3. 选择 `Run 'ParkingApplication.main()'`

### 步骤 4：查看启动结果

在底部的 `Run` 窗口中，你会看到：

```
  .   ____          _            __ _ _
 /\\ / ___'_ __ _ _(_)_ __  __ _ \ \ \ \
( ( )\___ | '_ | '_| | '_ \/ _` | \ \ \ \
 \\/  ___)| |_)| | | | | || (_| |  ) ) ) )
  '  |____| .__|_| |_|_| |_\__, | / / / /
 =========|_|==============|___/=/_/_/_/
 :: Spring Boot ::                (v2.7.18)

... (启动日志)

Started ParkingApplication in X.XXX seconds
```

看到 `Started ParkingApplication` 就表示启动成功了！✅

## 🎯 使用 Eclipse

### 步骤 1：导入项目

1. 打开 Eclipse
2. 选择 `File` → `Import`
3. 选择 `Maven` → `Existing Maven Projects`
4. 点击 `Next`
5. 选择 `d:\A_WORK\GraduationProject\done\backend` 文件夹
6. 点击 `Finish`

### 步骤 2：运行项目

1. 在项目树中找到：`src/main/java/com/parking/ParkingApplication.java`
2. **右键点击** `ParkingApplication.java`
3. 选择 `Run As` → `Java Application`

## 🎯 使用 VS Code

### 步骤 1：安装扩展

安装以下扩展：
- **Extension Pack for Java**（包含 Java 开发所需的所有扩展）
- **Spring Boot Extension Pack**（可选，但推荐）

### 步骤 2：打开项目

1. 打开 VS Code
2. 选择 `File` → `Open Folder`
3. 选择 `d:\A_WORK\GraduationProject\done\backend` 文件夹

### 步骤 3：运行项目

1. 打开 `src/main/java/com/parking/ParkingApplication.java`
2. 点击类名上方的 `Run` 按钮
3. 或者按 `F5` 启动调试

## ✅ 验证启动成功

无论使用哪种 IDE，启动成功后：

1. **查看控制台输出**
   - 应该看到 `Started ParkingApplication in X.XXX seconds`

2. **浏览器测试**
   - 访问：`http://localhost:8080/api/community/list`
   - 如果返回 JSON 数据，说明启动成功！

3. **检查端口**
   - 在 IDE 的控制台或日志中，应该看到服务运行在 8080 端口

## 🔧 IDE 启动的优势

✅ **自动处理环境变量**：不需要手动配置 PATH  
✅ **自动下载依赖**：Maven 依赖会自动下载  
✅ **更好的错误提示**：IDE 会显示详细的错误信息  
✅ **调试功能**：可以设置断点进行调试  
✅ **代码提示**：有完整的代码补全和提示  

## 🐛 常见问题

### Q: IDEA 提示 "SDK is not specified"？

**解决：**
1. `File` → `Project Structure` (或按 `Ctrl+Alt+Shift+S`)
2. 在 `Project` 标签页，设置 `Project SDK` 为已安装的 JDK
3. 如果没有 JDK，点击 `New` → 选择 JDK 安装目录

### Q: Eclipse 提示找不到 Maven？

**解决：**
1. `Window` → `Preferences`
2. `Maven` → `Installations`
3. 点击 `Add`，选择 Maven 安装目录
4. 勾选新添加的 Maven

### Q: 启动失败，提示数据库连接错误？

**检查：**
1. MySQL 服务是否启动
2. `application.yml` 中的数据库配置
3. 数据库 `parking_share` 是否已创建

## 📝 总结

**推荐使用 IDE 启动**，因为：
- ✅ 不需要配置 PATH 环境变量
- ✅ IDE 会自动处理所有依赖
- ✅ 有更好的错误提示和调试功能

**最简单的步骤：**
1. 用 IntelliJ IDEA 打开 `backend` 文件夹
2. 右键 `ParkingApplication.java` → `Run`
3. 完成！
